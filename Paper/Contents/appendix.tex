\section{Detailed Proof of Theorem~\ref{thm:security}}
\label{app:security-proof}

\begin{proof}
We establish completeness and soundness for the interactive protocol, and then
explain the Fiat--Shamir variant.

\paragraph{Perfect completeness}
Suppose the prover is honest and $\mat{A}\mat{B} = \mat{C}$.
Let $\mat{D} \coloneqq \mat{A}\mat{B} - \mat{C} = \mat{0}$.
For any challenge vector $\vect{r}$, we have $\vect{r}\mat{D}=\vect{0}$, so
\[
  \vect{y}
  = \vect{x}\mat{B}
  = (\vect{r}\mat{A})\mat{B}
  = \vect{r}(\mat{A}\mat{B})
  = \vect{r}\mat{C}
  = \vect{z}.
\]
For each intermediate vector $v\in\{\vect{x},\vect{y},\vect{z}\}$, the honest
prover commits to the intermediate vector before $I$ is sampled, commits to the
encoded leaf handles, publishes SNARK-side commitments through the
CC-SNARK backend,
and supplies accepted Merkle openings and CP-Link proofs for the sampled
coordinates.
For any queried index $j\in[n]$, linearity of $\mathcal{C}$ gives
\[
  \fold(\enc(\mat{A})^{(j)},\vect{r})
  = \enc(\vect{r}\mat{A})_j
  = \enc(\vect{x})_j,
\]
and analogously for the $\mat{B}$ and $\mat{C}$ folds.
Since $\vect{y}=\vect{z}$, the equality check between their encoded openings
also holds at every coordinate.
All authenticated openings and backend checks are honest, so the verifier
accepts with probability one.

\paragraph{Bad-event decomposition}
Let $\mathsf{Acc}$ be the event that the verifier accepts a false committed
matrix statement, and assume $\mat{A}\mat{B}\neq\mat{C}$.
Define $\mathsf{Bad}_{\mathsf{SNARK}}$ as acceptance of a false in-circuit
SNARK instance, $\mathsf{Bad}_{\mathsf{cc}}$ as a
vector-binding or witness-to-leaf linking failure for an accepted sampled
object, and $\mathsf{Bad}_{\mathsf{bind}}$ as two inconsistent accepted
leaf-handle openings for the same root and position.
For the sampled-opening part of $\mathsf{Bad}_{\mathsf{cc}}$, condition on the
CC-SNARK binding property: for every accepted SNARK-side
commitment $\Gamma_{\chi,j}$ it obtains the sampled witness value
$a_{\chi,j}$ and opening randomness $\rho_{\chi,j}$ bound to that commitment.
The CP-Link batch tag is
$T_I=\Hash(\textsf{FMM-link-batch},\mathsf{Cm}_{ABC},\mathsf{Cm}_{XYZ},I,P_I)$,
where $P_I=\{(\chi,j,\Gamma_{\chi,j},L_{\chi,j})\}_{\chi,j}$.
Thus the auxiliary proof is tied to the same SNARK-side/leaf commitment pairs that appear in the
SNARK public statement and the Merkle openings.
A sampled linking failure occurs if
$\openV(\pi_{\chi,j},L_{\chi,j},\cm_\chi,j)=1$ and
$\cpLinkV(\para,T_I,\{(\Gamma_{\chi,j},L_{\chi,j})\}_{\chi,j},\tau_I)=1$, but
for some sampled pair there is no leaf-opening randomness $\eta_{\chi,j}$ such
that $L_{\chi,j}$ opens to the same $a_{\chi,j}$ extracted from the Groth16
witness.
For the Pedersen handle instantiation this means there is no $\eta_{\chi,j}$
with
\begin{align*}
  L_{\chi,j}
  &= \eta_{\chi,j}H+
  \sum_{\ell=1}^{d_\chi} (a_{\chi,j})_\ell G_\ell,\\
  \Gamma_{\chi,j}-L_{\chi,j}
  &= (\rho_{\chi,j}-\eta_{\chi,j})H .
\end{align*}
Merkle position binding is charged separately to
$\mathsf{Bad}_{\mathsf{bind}}$; outside that event, the accepted
$L_{\chi,j}$ is the unique handle at position $j$ under the pre-query root
$\cm_\chi$.
By the assumed backend properties,
\begin{align*}
\Pr[\mathsf{Bad}_{\mathsf{SNARK}}]
  &\le \epsilon_{\mathsf{SNARK}}(\lambda),\\
\Pr[\mathsf{Bad}_{\mathsf{cc}}]
  &\le \epsilon_{\mathsf{cc}}(\lambda),\\
\Pr[\mathsf{Bad}_{\mathsf{bind}}]
  &\le \epsilon_{\mathsf{bind}}(\lambda).
\end{align*}
Conditioned on the complement of these events, every accepting transcript has a
valid SNARK witness, every accepted intermediate vector commitment binds a
single vector $v$ before $I$ is sampled, every sampled value used by the
Groth16 fold relation is bound to a SNARK-side commitment, every accepted
auxiliary opening is linked to that commitment, and every accepted leaf handle is
uniquely determined by its root and queried coordinate.
The input roots are fixed before the Freivalds challenge is sampled,
and the intermediate commitments are fixed before the sampled query tuple is
sampled.

\paragraph{Freivalds randomization}
Under the same conditioning, the matrices bound to
$\cm_A,\cm_B,\cm_C$ are fixed and satisfy
$\mat{A}\mat{B}\neq\mat{C}$.
Let $\mat{D}=\mat{A}\mat{B}-\mat{C}\neq 0$.
There exists a column $\vect{d}_j$ of $\mat{D}$ with $\vect{d}_j\neq 0$.
The condition $\vect{r}\mat{D}=0$ implies
\[
  P_j(r) \coloneqq
  \langle \vect{r},\vect{d}_j\rangle
  = \sum_{i=0}^{k-1}(\mat{D})_{i,j} r^i
  = 0 .
\]
Since $P_j$ is a nonzero polynomial of degree at most $k-1$ and $r$ is sampled
after the input commitments are fixed, Lemma~\ref{lem:sz} gives
\[
  \Pr_{r\sample\mathbb{F}}[\vect{r}\mat{D}=0]
  \le \Pr_{r\sample\mathbb{F}}[P_j(r)=0]
  \le \frac{k-1}{|\mathbb{F}|}.
\]

\paragraph{Proximity sampling}
Now condition further on $\vect{r}\mat{D}\neq 0$.
No vectors can simultaneously satisfy all four global relations:
\[
  \vect{x}=\vect{r}\mat{A},\quad
  \vect{y}=\vect{x}\mat{B},\quad
  \vect{z}=\vect{r}\mat{C},\quad
  \vect{y}=\vect{z}.
\]
Otherwise the four equalities would imply
$\vect{r}\mat{A}\mat{B}=\vect{r}\mat{C}$, contradicting
$\vect{r}\mat{D}\neq 0$.
Thus at least one relation is false.
Consider $\vect{x}\neq\vect{r}\mat{A}$; the other cases are identical.
Injectivity and distance of the code imply that the two codewords
$\enc(\vect{x})$ and $\enc(\vect{r}\mat{A})$ disagree on at least
$\delta n$ coordinates.
By code linearity, the sampled fold value from the committed rows of
$\mat{A}$ equals $\enc(\vect{r}\mat{A})_j$ at coordinate $j$.
Since the sampled column used by Groth16 is bound to the authenticated input
leaf handle, and the sampled vector value is both bound to its authenticated
leaf handle and checked to equal the circuit-computed codeword
$\enc(\vect{x})_j$, the fold check rejects whenever $j$ is in the disagreement
set.
A single uniformly sampled coordinate misses this disagreement set with
probability at most $1-\delta$, and $t$ independent sampled coordinates all
miss it with probability at most $(1-\delta)^t$.
Union-bounding over the four possible violated relations yields
$4(1-\delta)^t$.

\paragraph{Combining the bounds and parameters}
Summing the backend, Freivalds, and proximity errors gives
\[
\begin{aligned}
\Pr[\mathsf{Acc}\mid\mat{A}\mat{B}\neq\mat{C}]
&\le
\epsilon_{\mathsf{SNARK}}(\lambda)
+\epsilon_{\mathsf{cc}}(\lambda)
+\epsilon_{\mathsf{bind}}(\lambda)\\
&\quad+\frac{k-1}{|\mathbb{F}|}+4(1-\delta)^t .
\end{aligned}
\]
For $\lambda=128$, a 256-bit field makes $(k-1)/|\mathbb{F}|$ negligible for
the reported dimensions.
The proximity term is at most $2^{-\lambda}$ when
$t=\lceil(\lambda+2)/\log_2(1/(1-\delta))\rceil$; for example, for a code with
$\delta=1/2$, $t=130$ suffices for the proximity term alone.
For a conservative aggregate 128-bit budget over all terms, one should allocate
the budget across terms, e.g., by targeting each term at $2^{-131}$.

\paragraph{Fiat--Shamir variant}
The non-interactive transcript derives both $r$ and $I$ from
domain-separated hashes of the transcript prefix, including the public input
roots, intermediate vector commitments, and encoded-vector roots.
In the random oracle model, the preceding public-coin proof applies with the
standard Fiat--Shamir loss $\epsilon_{\mathsf{FS}}(q_H)$ for an adversary that
makes $q_H$ random-oracle queries.
This proves Theorem~\ref{thm:security}.
\end{proof}

\section{CP-Link Backend Instantiation}
\label{app:cplink-backend}

The artifact uses Pedersen-style~\cite{pedersen1991non} handles
\[
  \Gamma=\rho_\Gamma H+\sum_{\ell=1}^{d_\chi} a_\ell G_\ell ,
  \qquad
  L=\rho_L H+\sum_{\ell=1}^{d_\chi} a_\ell G_\ell ,
\]
with $d_\chi=k$ for matrix columns and $d_\chi=1$ for vector coordinates.
Leaf handles are authenticated by the Merkle tree, while the CC-SNARK backend
binds $\Gamma$ to the sampled witness value used by the circuit.
The link proof shows that the SNARK-side commitment and the authenticated leaf
handle open to the same sampled object.

\begin{theorem}[Batched CP-Link backend]\label{thm:cp-link-backend}
Assume the Pedersen handle parameters are generated so that the handle
commitment is binding, the QA-NIZK link proof for the linear batch relation is
sound, and the batching challenge is sampled by the Fiat--Shamir random oracle
after the batch statement is fixed.
For $Q=|\mathcal{S}|t$ sampled objects, the batched CP-Link verifier accepts a
false batch with probability at most
$\epsilon_{\mathsf{ped}}+\epsilon_{\mathsf{QA}}+Q/|\mathbb{F}|$.
The artifact serializes the $Q$ logical relations as one online batched proof;
generator and CRS bytes are setup material and are not included in the online
proof-size claim.
\end{theorem}

The logical CP-Link relation contains one equation per sampled object:
\[
  \Gamma_q-L_q=\Delta_qH,\qquad q\in[Q],
\]
where $Q=|\mathcal{S}|t$ and $q$ indexes the pairs
$(\chi,j)\in\mathcal{S}\times I$.
The batch statement is
\[
\begin{aligned}
P_I&=\{(\chi_q,j_q,\Gamma_q,L_q)\}_{q=1}^{Q},\\
T_I&=\Hash(\textsf{FMM-link-batch},\mathsf{Cm}_{ABC},\mathsf{Cm}_{XYZ},I,P_I).
\end{aligned}
\]
The implementation batches these equations by deriving
$\beta\gets\Hash(\textsf{FMM-link-rand},T_I,\{\Gamma_q,L_q\}_{q=1}^{Q})$ and
checking a single randomized linear relation
\[
  \sum_{q=1}^{Q}\beta^q(\Gamma_q-L_q)=
  \left(\sum_{q=1}^{Q}\beta^q\Delta_q\right)H .
\]
If any individual equality is false, the left-minus-right side is a nonzero
degree-$Q$ polynomial in $\beta$, so the batch can pass with probability at
most $Q/|\mathbb{F}|$ in the random-oracle model.
Combining this with Pedersen binding and QA-NIZK soundness gives the bound in
Theorem~\ref{thm:cp-link-backend}.
The QA-NIZK statement is
$(\para,T_I,\{\Gamma_q,L_q\}_{q=1}^{Q})$ and the witness is the aggregate
difference opening $\sum_q\beta^q\Delta_q$.
Its CRS fixes the handle generators and linear-relation verification
parameters; if serialized, those bytes are setup/key/CRS material rather than
online proof bytes.
Thus the $128$-byte value reported in the experiments is one online batched
QA-NIZK proof for all sampled links, not a per-link cost.

\section{Additional Evaluation Details}
\label{app:eval-details}

This appendix records the measurement boundary behind the main evaluation.
Tables~\ref{tab:eval-parameters} and~\ref{tab:metric-coverage} first state the
fixed experimental parameters and which costs are covered by the artifact.
Tables~\ref{tab:proof-material-accounting}--\ref{tab:constraint-accounting}
then separate serialized proof material from private Groth16 witness data and
from costs that remain unmeasured, such as raw-matrix encoding/root construction and
serialized key/CRS size.
The intent is to make the main text's proof-size and speedup claims auditable
without treating the checking-layer measurements as full end-to-end VC costs.

Table~\ref{tab:eval-parameters} fixes the common parameter choices used by the
reported rows. In particular, the measured rows use $t=128$ for scaling, while
the table also records the nearby query counts needed for stricter
proximity-only and per-term security budgets.

\begin{table}[H]
\centering
\small
\begin{tabularx}{\columnwidth}{lX}
\toprule
\textbf{Parameter} & \textbf{Value in main experiments} \\
\midrule
Field size & 256-bit prime field \\
Server & AMD EPYC 7B13, 32 cores, 251.69\,GiB RAM, Linux/amd64 \\
Matrix dimensions & $\fmQA$: $2^7$--$2^{13}$; Freivalds: $2^7$--$2^{12}$ \\
Code rate & $\rho=1/2$ \\
Codeword length & $n=2k$ \\
Sampled positions & $t=128$ \\
Primary linking layer & Pre-query vector commitments, SNARK-side commitments, QA-NIZK/CP-Link, and multi-column Merkle openings \\
Backend model & CP-Link sampled intermediate backend; input roots are fixed before $r$ \\
Query-count targets & Measured rows use $t=128$; proximity-only $2^{-128}$ at $\delta=1/2$ uses $t=130$; conservative per-term $2^{-131}$ uses $t=133$. \\
\bottomrule
\end{tabularx}
\caption{Concrete parameters used in the main experiments.}
\label{tab:eval-parameters}
\end{table}

Table~\ref{tab:metric-coverage} is a measurement-scope checklist rather than a
performance result. It distinguishes quantities measured directly by the CSV
artifact from costs that remain outside the online proof bytes, most notably
raw-matrix encoding/root construction and serialized key/CRS sizes.

\begin{table}[H]
\centering
\small
\begin{tabularx}{\columnwidth}{@{}p{0.31\columnwidth}p{0.17\columnwidth}X@{}}
\toprule
\textbf{Metric} & \textbf{Covered?} & \textbf{Interpretation} \\
\midrule
Constraint count & Yes, aggregate & Groth16 circuit size including vector openings, sampled RS evaluations, and fold checks; per-gadget constraint counters are not reported, and CC-SNARK/linking overhead is measured in time/proof bytes rather than as extra R1CS constraints. \\
Online proving time & Yes & Excludes native matrix multiplication and one-time setup. \\
Verification time & Yes & Includes current QA-NIZK/CP-Link and opening verification overhead. \\
Proof size & Yes & Includes Groth16, compact sampled commitments/openings, and QA-NIZK material; raw sampled matrix columns are Groth16 witness data, not serialized proof bytes. \\
Setup time & Partial & Recorded for the matrix and GPT-2 workloads; setup/key sizes are not reported. \\
Input-root generation & Partial & Root construction is outside online proof bytes; raw-matrix encoding/root costs must be added for end-to-end deployments. \\
Peak memory and variance & No & Needed for a full systems artifact evaluation. \\
\bottomrule
\end{tabularx}
\caption{Measurement coverage in the current artifact.}
\label{tab:metric-coverage}
\end{table}

Table~\ref{tab:proof-material-accounting} explains the proof-size convention
used in the main text. The sampled matrix columns are large, but they are
private Groth16 witness data; the serialized proof contains only compact
handles, authentication paths, the Groth16 proof, and the batched CP-Link
material.

\begin{table}[H]
\centering
\scriptsize
\setlength{\tabcolsep}{2.5pt}
\begin{tabularx}{\columnwidth}{@{}p{0.30\columnwidth}p{0.15\columnwidth}X@{}}
\toprule
\textbf{Object} & \textbf{Serialized?} & \textbf{Where it is checked / charged} \\
\midrule
Input column $a_{M,j}$ & No & $k$ field elements are Groth16 witness data; handles and paths are serialized. \\
Vector leaf $a_{v,j}$ & No & One field element is used as Groth16 witness data and checked by $a_{v,j}=\enc(v)_j$; the SNARK-side commitment is linked to the Merkle leaf handle. \\
Merkle path & Yes & Authenticates $L_{\chi,j}$ in $\mathsf{AuxV}$; charged to proof bytes and verify time. \\
CP-Link proof & Yes & One batched QA-NIZK proof $\tau_I$ links all sampled SNARK-side/leaf commitment pairs. \\
Groth16 proof & Yes & Verifies vector openings, sampled RS evaluations, and fold/equality checks, while its CC-SNARK interface binds sampled values to SNARK-side commitments. \\
\bottomrule
\end{tabularx}
\caption{Proof-material accounting for the CP-Link sampled-opening backend.}
\label{tab:proof-material-accounting}
\end{table}

Table~\ref{tab:byte-accounting} instantiates the accounting for two
representative matrix sizes. The reported totals are the serialized online
proof bytes in the artifact; roots and vector commitments are public inputs,
and raw sampled columns are intentionally not serialized.

\begin{table}[H]
\centering
\scriptsize
\setlength{\tabcolsep}{3pt}
\begin{tabularx}{\columnwidth}{@{}p{0.16\columnwidth}rrX@{}}
\toprule
\textbf{Case} & \textbf{Opening payload} & \textbf{Online proof} & \textbf{Note} \\
\midrule
$k=1{,}024$ & $108{,}160$\,B & $108{,}548$\,B & Adds $260$\,B Groth16 and $128$\,B batched CP-Link; roots/vector commitments are public inputs. \\
$k=4{,}096$ & $146{,}400$\,B & $146{,}788$\,B & Raw sampled columns would be $50.3$\,MB if serialized. \\
\bottomrule
\end{tabularx}
\caption{Representative byte-level accounting from
\texttt{meow\_benchmark\_results.csv}.}
\label{tab:byte-accounting}
\end{table}

Table~\ref{tab:setup-accounting} records setup time for representative rows and
separates protocol, circuit, and CP-Link setup components. The current artifact
does not report serialized proving/verifying-key or CRS bytes; this is why the
main text states proof size as online proof material excluding key/CRS data.

\begin{table}[H]
\centering
\scriptsize
\setlength{\tabcolsep}{4pt}
\begin{tabularx}{\columnwidth}{@{}p{0.19\columnwidth}rrrrX@{}}
\toprule
\textbf{Case} & \textbf{Total setup} & \textbf{Protocol} & \textbf{Circuit} & \textbf{CP-Link} & \textbf{Key / CRS size} \\
\midrule
$k=1{,}024$ & $32.99$\,s & $0.06$\,s & $31.81$\,s & $1.12$\,s & Not reported by the current artifact. \\
$k=4{,}096$ & $128.21$\,s & $0.23$\,s & $123.64$\,s & $4.34$\,s & Not reported by the current artifact. \\
\bottomrule
\end{tabularx}
\caption{Setup accounting for representative rows.}
\label{tab:setup-accounting}
\end{table}

Table~\ref{tab:constraint-accounting} separates the dominant deterministic
$3tk$ fold terms from the remaining checked constraints. The residual includes
sampled RS evaluations, vector openings, equality checks, and circuit
glue, so the table is a structural audit of the aggregate constraint counts
rather than a per-gadget profiler.

\begin{table}[H]
\centering
\scriptsize
\setlength{\tabcolsep}{3pt}
\begin{tabularx}{\columnwidth}{@{}p{0.17\columnwidth}rrrr@{}}
\toprule
\textbf{Case} & \textbf{Total} & \textbf{$3tk$ folds} & \textbf{Residual} & \textbf{Raw cols.} \\
\midrule
$k=1{,}024$ & $419{,}139$ & $393{,}216$ & $25{,}923$ & $12.6$\,MB \\
$k=4{,}096$ & $1{,}667{,}305$ & $1{,}572{,}864$ & $94{,}441$ & $50.3$\,MB \\
\bottomrule
\end{tabularx}
\caption{Structural constraint and raw-column-size accounting for two
representative rows.}
\label{tab:constraint-accounting}
\end{table}

The accounting tables should be read as lower-level support for the main
evaluation, not as additional benchmark claims.
They justify three statements used throughout the paper: the large sampled
columns remain private witness data, the serialized online proof is dominated
by compact opening material rather than raw columns, and the current artifact
still leaves key/CRS size and raw-matrix encoding/root construction as
deployment costs outside the online proof-byte measurements.
