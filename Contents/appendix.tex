\section{Detailed Proof of Theorem~\ref{thm:security}}
\label{app:security-proof}

\begin{proof}
We prove the public-coin interactive protocol first.  The Fiat--Shamir variant
is handled at the end.

\paragraph{Perfect completeness}
Assume that the honest prover holds matrices
\(\mat{A},\mat{B},\mat{C}\in\mathbb{F}^{k\times k}\) with
\(\mat{A}\mat{B}=\mat{C}\).  Let
\(\vect{r}=(1,r,\ldots,r^{k-1})\), and define
\[
  \vect{x}=\vect{r}\mat{A},
  \qquad
  \vect{y}=\vect{x}\mat{B},
  \qquad
  \vect{z}=\vect{r}\mat{C}.
\]
Then \(\vect{y}=\vect{z}\).  The honest prover commits to the packed encoded
matrix blocks \(\{\vect{m}_{ABC,j}\}_{j\in[n]}\) under \(\rt_{ABC}\), commits
to the packed encoded vector blocks \(\{\vect{m}_{XYZ,j}\}_{j\in[n]}\) under
\(\rt_{XYZ}\), and supplies the sampled openings and CP-Link proofs required
by Section~\ref{sec:openings-and-cplink}.

For every queried position \(j\in I\), code linearity gives
\[
  \fold(\vect{r},\widehat{\mat{A}}[*,j])
  =
  \widehat{\vect{x}}[j],
\]
and similarly
\[
  \fold(\vect{x},\widehat{\mat{B}}[*,j])
  =
  \widehat{\vect{y}}[j],
  \qquad
  \fold(\vect{r},\widehat{\mat{C}}[*,j])
  =
  \widehat{\vect{z}}[j].
\]
Since \(\vect{y}=\vect{z}\), the equality check
\(\widehat{\vect{y}}[j]=\widehat{\vect{z}}[j]\) also holds.  All SNARK,
Merkle, and CP-Link checks are honestly generated, so the verifier accepts.

\paragraph{Bad-event decomposition}
Let \(\mathsf{Acc}\) be the event that the verifier accepts a false committed
matrix multiplication statement.  We decompose the failure probability into
the following events:
\begin{itemize}
  \item \(\mathsf{Bad}_{\mathsf{SNARK}}\): the CP-SNARK accepts although the
  sampled relation of Section~\ref{sec:snark-relation} is false for the
  committed sampled witnesses;
  \item \(\mathsf{Bad}_{\mathsf{open}}\): a Merkle opening or Pedersen leaf
  opening is accepted in a way that violates position binding or commitment
  binding;
  \item \(\mathsf{Bad}_{\mathsf{link}}\): a CP-Link proof accepts although the
  SNARK-side sampled blocks are not the same messages as the opened Pedersen
  leaves under \(\rt_{ABC}\) and \(\rt_{XYZ}\).
  \item \(\mathsf{Bad}_{\mathsf{code}}\): the probabilistic code-consistency
  checks accept encoded views that are not encodings of their corresponding
  intermediate vectors.
\end{itemize}
By the assumed soundness of the selected backends,
\[
\begin{aligned}
\Pr[\mathsf{Bad}_{\mathsf{SNARK}}]
  &\le \epsilon_{\mathsf{cp}}(\lambda),\\
\Pr[\mathsf{Bad}_{\mathsf{open}}]
  &\le \epsilon_{\mathsf{open}}(\lambda),\\
\Pr[\mathsf{Bad}_{\mathsf{link}}]
  &\le \epsilon_{\mathsf{link}}(\lambda),\\
\Pr[\mathsf{Bad}_{\mathsf{code}}]
  &\le \epsilon_{\mathsf{code}}(\lambda).
\end{aligned}
\]
Condition on the complement of these events.  Then \(\rt_{ABC}\) fixes a
unique sequence of packed matrix-column commitments at all queried positions,
\(\rt_{XYZ}\) fixes a unique sequence of packed vector-symbol commitments at
all queried positions, and the sampled values used by the SNARK are exactly the
messages opened from those commitments.  In particular, the adversary cannot
choose different sampled blocks after seeing \(I\) without breaking the
opening or linking layer.

\paragraph{Freivalds randomization}
The matrix root \(\rt_{ABC}\) is fixed before \(r\) is derived.  In the
committed statement, \(\rt_{ABC}\) binds to row-wise encoded matrices
\(\mat{A},\mat{B},\mat{C}\).  Suppose
\(\mat{A}\mat{B}\neq\mat{C}\), and let
\(\mat{D}=\mat{A}\mat{B}-\mat{C}\neq 0\).  Some column
\(\vect{d}\) of \(\mat{D}\) is nonzero.  The event
\(\vect{r}\mat{D}=0\) implies
\[
  P(r)
  \coloneqq
  \langle (1,r,\ldots,r^{k-1}),\vect{d}\rangle
  =
  \sum_{i=0}^{k-1} d_i r^i
  =
  0 .
\]
The polynomial \(P\) is nonzero and has degree at most \(k-1\).  Since \(r\)
is sampled after \(\rt_{ABC}\) is fixed, Schwartz--Zippel gives
\[
  \Pr[\vect{r}\mat{D}=0]
  \le
  \frac{k-1}{|\mathbb{F}|}.
\]

\paragraph{Proximity sampling}
Now also condition on \(\vect{r}\mat{D}\neq 0\).  No vectors
\(\vect{x},\vect{y},\vect{z}\) can satisfy all four global relations
\[
  \vect{x}=\vect{r}\mat{A},
  \qquad
  \vect{y}=\vect{x}\mat{B},
  \qquad
  \vect{z}=\vect{r}\mat{C},
  \qquad
  \vect{y}=\vect{z}.
\]
Otherwise they would imply
\(\vect{r}\mat{A}\mat{B}=\vect{r}\mat{C}\), contradicting
\(\vect{r}\mat{D}\neq 0\).

Therefore at least one checked encoded relation is false.  Consider the case
\(\vect{x}\neq\vect{r}\mat{A}\); the other three cases are identical.  The
codewords \(\enc(\vect{x})\) and \(\enc(\vect{r}\mat{A})\) are distinct, so by
relative distance they disagree on at least \(\delta n\) coordinates.  For any
queried coordinate \(j\), code linearity gives
\[
  \enc(\vect{r}\mat{A})[j]
  =
  \fold(\vect{r},\widehat{\mat{A}}[*,j]).
\]
The sampled-opening backend ensures that the SNARK uses the opened
\(\widehat{\mat{A}}[*,j]\) block under \(\rt_{ABC}\) and the opened
\(\widehat{\vect{x}}[j]\) symbol under \(\rt_{XYZ}\).  Thus the fold check
rejects whenever \(j\) lands in the disagreement set.  A uniformly sampled
coordinate misses this set with probability at most \(1-\delta\), and \(t\)
independent samples miss it with probability at most \((1-\delta)^t\).
Union-bounding over the four possible violated relations gives
\[
  4(1-\delta)^t .
\]

\paragraph{Combining the bounds}
Adding the SNARK, opening, linking, code-consistency, Freivalds, and proximity
failure events gives
\[
\begin{aligned}
\Pr[\mathsf{Acc}\mid\mat{A}\mat{B}\neq\mat{C}]
&\le
\epsilon_{\mathsf{cp}}(\lambda)
+\epsilon_{\mathsf{open}}(\lambda)
+\epsilon_{\mathsf{link}}(\lambda)
+\epsilon_{\mathsf{code}}(\lambda)\\
&\quad+\frac{k-1}{|\mathbb{F}|}+4(1-\delta)^t .
\end{aligned}
\]
For the measured rows, \(t=128\).  When \(\delta=1/2\), the proximity term is
\(4\cdot2^{-128}=2^{-126}\).  Larger \(t\) or a larger-distance code can be
used when the deployment target requires a smaller proximity term.

\paragraph{Fiat--Shamir variant}
The non-interactive protocol derives \(r\), \(I\), and backend batch challenges
from domain-separated hashes of the transcript.  In the random oracle model,
the public-coin proof above applies with the standard Fiat--Shamir loss
\(\epsilon_{\mathsf{FS}}(q_H)\) for an adversary making \(q_H\)
random-oracle queries.
This proves Theorem~\ref{thm:security}.
\end{proof}

\section{Barycentric Reed--Solomon Consistency Check}
\label{app:rs-barycentric}

This appendix specifies the Reed--Solomon instantiation of
\(\mathsf{EncCheck}_{\mathcal{C}}\) used in
Section~\ref{sec:snark-relation}.  Let
\[
  D=\{\omega_1,\ldots,\omega_n\}\subset\mathbb{F}
\]
be the public evaluation domain.  A message
\(\vect{v}=(v_0,\ldots,v_{k-1})\in\mathbb{F}^k\) defines the degree-\(<k\)
polynomial
\[
  p_{\vect{v}}(X)=\sum_{\ell=0}^{k-1} v_\ell X^\ell,
  \qquad
  \enc(\vect{v})=(p_{\vect{v}}(\omega_i))_{i=1}^{n}.
\]
Given a claimed encoded word
\(\widehat{\vect{v}}\in\mathbb{F}^n\), let
\(P_{\widehat{\vect{v}}}\) be the unique degree-\(<n\) polynomial satisfying
\[
  P_{\widehat{\vect{v}}}(\omega_i)=\widehat{\vect{v}}[i]
  \qquad\text{for all }i\in[n].
\]
The check samples \(\alpha\sample\mathbb{F}\setminus D\) after the claimed
word is fixed and compares \(P_{\widehat{\vect{v}}}(\alpha)\) with
\(p_{\vect{v}}(\alpha)\).

For efficient evaluation, define the domain polynomial and barycentric weights
\[
  L_D(X)=\prod_{i=1}^{n}(X-\omega_i),
  \qquad
  \lambda_i
  =
  \left(\prod_{\ell\neq i}(\omega_i-\omega_\ell)\right)^{-1}.
\]
For every \(\alpha\notin D\), barycentric interpolation gives
\[
  P_{\widehat{\vect{v}}}(\alpha)
  =
  L_D(\alpha)
  \sum_{i=1}^{n}
  \lambda_i
  \frac{\widehat{\vect{v}}[i]}{\alpha-\omega_i}.
\]
Thus the concrete Reed--Solomon consistency check is
\[
\begin{aligned}
\mathsf{EncCheck}_{\mathsf{RS}}
(\widehat{\vect{v}},\vect{v};\alpha)=1
\Longleftrightarrow{}&
\sum_{\ell=0}^{k-1} v_\ell \alpha^\ell
\\
&=
L_D(\alpha)
\sum_{i=1}^{n}
\lambda_i
\frac{\widehat{\vect{v}}[i]}{\alpha-\omega_i}.
\end{aligned}
\]
The right-hand side is a linear form in the claimed encoded symbols once
\(\alpha\) is fixed.  Equivalently, the verifier can derive the interpolation
coefficients
\[
  \gamma_i(\alpha)
  =
  L_D(\alpha)\lambda_i(\alpha-\omega_i)^{-1}
\]
and the circuit checks
\[
  \sum_{\ell=0}^{k-1} v_\ell \alpha^\ell
  =
  \sum_{i=1}^{n}\gamma_i(\alpha)\widehat{\vect{v}}[i].
\]
This costs \(\mathcal{O}(k+n)\) field operations per checked point, after the
domain-dependent weights are precomputed.

\begin{lemma}[Barycentric code-consistency soundness]
\label{lem:rs-barycentric-soundness}
If \(\widehat{\vect{v}}\neq\enc(\vect{v})\) and
\(\alpha\sample\mathbb{F}\setminus D\) is sampled after
\(\widehat{\vect{v}}\) and \(\vect{v}\) are fixed, then
\[
  \Pr[
    \mathsf{EncCheck}_{\mathsf{RS}}
    (\widehat{\vect{v}},\vect{v};\alpha)=1
  ]
  \le
  \frac{n-1}{|\mathbb{F}|-n}.
\]
\end{lemma}

\begin{proof}
If \(\widehat{\vect{v}}\neq\enc(\vect{v})\), then the interpolation polynomial
\(P_{\widehat{\vect{v}}}\) differs from the degree-\(<k\) polynomial
\(p_{\vect{v}}\).  Their difference
\[
  Q(X)=P_{\widehat{\vect{v}}}(X)-p_{\vect{v}}(X)
\]
is a nonzero polynomial of degree at most \(n-1\).  The check accepts exactly
when \(Q(\alpha)=0\).  Since \(\alpha\) is uniform over
\(\mathbb{F}\setminus D\), at most \(n-1\) choices of \(\alpha\) can make the
check accept, which proves the bound.
\end{proof}

In \(\protocol\), this check is applied independently to
\(\widehat{\vect{x}},\widehat{\vect{y}},\widehat{\vect{z}}\) at
\(\alpha_x,\alpha_y,\alpha_z\).  A union bound gives the
\(3(n-1)/(|\mathbb{F}|-n)\) term used in
Definition~\ref{def:lamp-backend-assumptions}.

% \section{CP-Link Backend Instantiation}
% \label{app:cplink-backend}

% This appendix records the concrete blockwise CP-Link relation used by the
% artifact.  For each queried \(j\in I\), the external Pedersen leaves have the
% form
% \[
% \begin{aligned}
%   \cm_{ABC,j}
%   &=
%   \pedC(\ck_{ABC},\vect{m}_{ABC,j};o_{ABC,j}),\\
%   \cm_{XYZ,j}
%   &=
%   \pedC(\ck_{XYZ},\vect{m}_{XYZ,j};o_{XYZ,j}).
% \end{aligned}
% \]
% The CP-SNARK committed-witness interface produces
% \(\widetilde{\cm}_{ABC}\) for the concatenated sampled matrix blocks
% \((\vect{m}_{ABC,j})_{j\in I}\), and \(\widetilde{\cm}_{XYZ}\) for the
% concatenated sampled vector blocks \((\vect{m}_{XYZ,j})_{j\in I}\).  The
% CP-Link proof shows that these SNARK-side commitments and the opened external
% Pedersen leaves contain the same block messages.

% \begin{theorem}[Batched CP-Link backend]\label{thm:cp-link-backend}
% Assume the Pedersen parameters are binding, the QA-NIZK proof for the linear
% batch relation is sound, and the batching challenge is sampled by the
% Fiat--Shamir random oracle after the batch statement is fixed.  For the two
% sampled block families \(ABC\) and \(XYZ\), each with \(t\) queried blocks, the
% batched CP-Link verifier accepts a false batch with probability at most
% \[
%   \epsilon_{\mathsf{ped}}
%   +\epsilon_{\mathsf{QA}}
%   +\frac{2t}{|\mathbb{F}|}.
% \]
% Generator and CRS bytes are setup material and are not included in the online
% proof-size claim.
% \end{theorem}

% The logical CP-Link statement contains one equality per opened block.  We index
% these \(Q=2t\) equalities by \(q\in[Q]\).  Each equality has the form
% \[
%   C^{\mathsf{snark}}_q
%   -
%   C^{\mathsf{leaf}}_q
%   =
%   \Delta_q H,
% \]
% where \(C^{\mathsf{snark}}_q\) is the commitment induced by the SNARK-side
% sampled-block commitment, \(C^{\mathsf{leaf}}_q\) is the corresponding external
% Pedersen leaf commitment, and \(\Delta_q\) is the difference of the two opening
% randomness values.  The batch statement is
% \[
% \begin{aligned}
% P_I&=\{(\mathrm{tag}_q,C^{\mathsf{snark}}_q,C^{\mathsf{leaf}}_q)\}_{q=1}^{Q},\\
% T_I&=\Hash(\textsf{LAMP-link-batch},\rt_{ABC},\rt_{XYZ},I,P_I).
% \end{aligned}
% \]
% The implementation derives
% \[
%   \beta
%   \gets
%   \Hash(\textsf{LAMP-link-rand},T_I,P_I)
% \]
% and checks the single randomized linear relation
% \[
%   \sum_{q=1}^{Q}\beta^q
%   (C^{\mathsf{snark}}_q-C^{\mathsf{leaf}}_q)
%   =
%   \left(\sum_{q=1}^{Q}\beta^q\Delta_q\right)H .
% \]
% If any individual equality is false after the batch statement is fixed, the
% left-minus-right side is a nonzero polynomial in \(\beta\) of degree at most
% \(Q\).  Hence the randomized batch can pass with probability at most
% \(Q/|\mathbb{F}|=2t/|\mathbb{F}|\).  Combining this batch error with Pedersen
% binding and QA-NIZK soundness gives Theorem~\ref{thm:cp-link-backend}.  The
% reported \(128\)-byte CP-Link proof is one online batched QA-NIZK proof for the
% whole sampled batch, not a per-link cost.

% \section{Additional Evaluation Details}
% \label{app:eval-details}

% This appendix records the measurement boundary behind the main evaluation.
% Tables~\ref{tab:eval-parameters} and~\ref{tab:metric-coverage} first state the
% fixed experimental parameters and which costs are covered by the artifact.
% Tables~\ref{tab:proof-material-accounting}--\ref{tab:constraint-accounting}
% then separate serialized proof material from private Groth16 witness data and
% from costs that remain unmeasured, such as raw-matrix encoding/root construction and
% serialized key/CRS size.
% The intent is to make the main text's proof-size and speedup claims auditable
% without treating the checking-layer measurements as full end-to-end VC costs.

% Table~\ref{tab:eval-parameters} fixes the common parameter choices used by the
% reported rows. In particular, the measured rows use $t=128$ for scaling, while
% the table also records the nearby query counts needed for stricter
% proximity-only and per-term security budgets.

% \begin{table}[H]
% \centering
% \small
% \begin{tabularx}{\columnwidth}{lX}
% \toprule
% \textbf{Parameter} & \textbf{Value in main experiments} \\
% \midrule
% Field size & 256-bit prime field \\
% Server & AMD EPYC 7B13, 32 cores, 251.69\,GiB RAM, Linux/amd64 \\
% Matrix dimensions & $\fmQA$: $2^7$--$2^{13}$; Freivalds: $2^7$--$2^{12}$ \\
% Code rate & $\rho=1/2$ \\
% Codeword length & $n=2k$ \\
% Sampled positions & $t=128$ \\
% Primary linking layer & Packed Merkle roots, SNARK-side sampled-block commitments, QA-NIZK/CP-Link, and multi-column Merkle openings \\
% Backend model & CP-Link sampled intermediate backend; input roots are fixed before $r$ \\
% Query-count targets & Measured rows use $t=128$; proximity-only $2^{-128}$ at $\delta=1/2$ uses $t=130$; conservative per-term $2^{-131}$ uses $t=133$. \\
% \bottomrule
% \end{tabularx}
% \caption{Concrete parameters used in the main experiments.}
% \label{tab:eval-parameters}
% \end{table}

% Table~\ref{tab:metric-coverage} is a measurement-scope checklist rather than a
% performance result. It distinguishes quantities measured directly by the CSV
% artifact from costs that remain outside the online proof bytes, most notably
% raw-matrix encoding/root construction and serialized key/CRS sizes.

% \begin{table}[H]
% \centering
% \small
% \begin{tabularx}{\columnwidth}{@{}p{0.31\columnwidth}p{0.17\columnwidth}X@{}}
% \toprule
% \textbf{Metric} & \textbf{Covered?} & \textbf{Interpretation} \\
% \midrule
% Constraint count & Yes, aggregate & Groth16 circuit size including sampled RS evaluations and fold checks; per-gadget constraint counters are not reported, and CP-SNARK/linking overhead is measured in time/proof bytes rather than as extra R1CS constraints. \\
% Online proving time & Yes & Excludes native matrix multiplication and one-time setup. \\
% Verification time & Yes & Includes current QA-NIZK/CP-Link and opening verification overhead. \\
% Proof size & Yes & Includes Groth16, compact sampled commitments/openings, and QA-NIZK material; raw sampled matrix columns are Groth16 witness data, not serialized proof bytes. \\
% Setup time & Partial & Recorded for the matrix and GPT-2 workloads; setup/key sizes are not reported. \\
% Committed-root generation & Partial & Construction of \(\rt_{ABC}\) and \(\rt_{XYZ}\) is outside online proof bytes; raw-matrix encoding/root costs must be added for end-to-end deployments. \\
% Peak memory and variance & No & Needed for a full systems artifact evaluation. \\
% \bottomrule
% \end{tabularx}
% \caption{Measurement coverage in the current artifact.}
% \label{tab:metric-coverage}
% \end{table}

% Table~\ref{tab:proof-material-accounting} explains the proof-size convention
% used in the main text. The sampled matrix columns are large, but they are
% private Groth16 witness data; the serialized proof contains only compact
% handles, authentication paths, the Groth16 proof, and the batched CP-Link
% material.

% \begin{table}[H]
% \centering
% \scriptsize
% \setlength{\tabcolsep}{2.5pt}
% \begin{tabularx}{\columnwidth}{@{}p{0.30\columnwidth}p{0.15\columnwidth}X@{}}
% \toprule
% \textbf{Object} & \textbf{Serialized?} & \textbf{Where it is checked / charged} \\
% \midrule
% Matrix block \(\vect{m}_{ABC,j}\) & No & The sampled block is Groth16 witness data; compact commitments and Merkle paths are serialized. \\
% Vector block \(\vect{m}_{XYZ,j}\) & No & The sampled block is Groth16 witness data and is checked against \(\widehat{\vect{x}}[j]\), \(\widehat{\vect{y}}[j]\), and \(\widehat{\vect{z}}[j]\); the SNARK-side sampled block is linked to the opened Pedersen leaf. \\
% Merkle path & Yes & Authenticates an opened Pedersen leaf under \(\rt_{ABC}\) or \(\rt_{XYZ}\); charged to proof bytes and verify time. \\
% CP-Link proof & Yes & One batched QA-NIZK proof \(\tau_I\) links the two sampled block families to their opened Pedersen leaves. \\
% Groth16 proof & Yes & Verifies sampled RS evaluations and fold/equality checks, while the CP-SNARK interface binds sampled block witnesses to SNARK-side commitments. \\
% \bottomrule
% \end{tabularx}
% \caption{Proof-material accounting for the CP-Link sampled-opening backend.}
% \label{tab:proof-material-accounting}
% \end{table}

% Table~\ref{tab:byte-accounting} instantiates the accounting for two
% representative matrix sizes. The reported totals are the serialized online
% proof bytes in the artifact; roots and vector commitments are public inputs,
% and raw sampled columns are intentionally not serialized.

% \begin{table}[H]
% \centering
% \scriptsize
% \setlength{\tabcolsep}{3pt}
% \begin{tabularx}{\columnwidth}{@{}p{0.16\columnwidth}rrX@{}}
% \toprule
% \textbf{Case} & \textbf{Opening payload} & \textbf{Online proof} & \textbf{Note} \\
% \midrule
% $k=1{,}024$ & $43{,}904$\,B & $44{,}292$\,B & Adds $260$\,B Groth16 and $128$\,B batched CP-Link; roots are public inputs. \\
% $k=4{,}096$ & $58{,}944$\,B & $59{,}332$\,B & Raw sampled columns would be $50.3$\,MB if serialized. \\
% \bottomrule
% \end{tabularx}
% \caption{Representative byte-level accounting from
% \texttt{meow\_benchmark\_results\_v2.csv}.}
% \label{tab:byte-accounting}
% \end{table}

% Table~\ref{tab:setup-accounting} records setup time for representative rows and
% separates protocol, circuit, and CP-Link setup components. The current artifact
% does not report serialized proving/verifying-key or CRS bytes; this is why the
% main text states proof size as online proof material excluding key/CRS data.

% \begin{table}[H]
% \centering
% \scriptsize
% \setlength{\tabcolsep}{4pt}
% \begin{tabularx}{\columnwidth}{@{}p{0.19\columnwidth}rrrrX@{}}
% \toprule
% \textbf{Case} & \textbf{Total setup} & \textbf{Protocol} & \textbf{Circuit} & \textbf{CP-Link} & \textbf{Key / CRS size} \\
% \midrule
% $k=1{,}024$ & $33.16$\,s & $0.18$\,s & $31.83$\,s & $1.15$\,s & Not reported by the current artifact. \\
% $k=4{,}096$ & $130.08$\,s & $0.71$\,s & $125.01$\,s & $4.37$\,s & Not reported by the current artifact. \\
% \bottomrule
% \end{tabularx}
% \caption{Setup accounting for representative rows.}
% \label{tab:setup-accounting}
% \end{table}

% Table~\ref{tab:constraint-accounting} separates the dominant deterministic
% $3tk$ fold terms from the remaining checked constraints. The residual includes
% sampled RS evaluations, equality checks, and circuit
% glue, so the table is a structural audit of the aggregate constraint counts
% rather than a per-gadget profiler.

% \begin{table}[H]
% \centering
% \scriptsize
% \setlength{\tabcolsep}{3pt}
% \begin{tabularx}{\columnwidth}{@{}p{0.17\columnwidth}rrrr@{}}
% \toprule
% \textbf{Case} & \textbf{Total} & \textbf{$3tk$ folds} & \textbf{Residual} & \textbf{Raw cols.} \\
% \midrule
% $k=1{,}024$ & $418{,}149$ & $393{,}216$ & $24{,}933$ & $12.6$\,MB \\
% $k=4{,}096$ & $1{,}666{,}315$ & $1{,}572{,}864$ & $93{,}451$ & $50.3$\,MB \\
% \bottomrule
% \end{tabularx}
% \caption{Structural constraint and raw-column-size accounting for two
% representative rows.}
% \label{tab:constraint-accounting}
% \end{table}

% The accounting tables should be read as lower-level support for the main
% evaluation, not as additional benchmark claims.
% They justify three statements used throughout the paper: the large sampled
% columns remain private witness data, the serialized online proof is dominated
% by compact opening material rather than raw columns, and the current artifact
% still leaves key/CRS size and raw-matrix encoding/root construction as
% deployment costs outside the online proof-byte measurements.
