# IEEE S&P 2027 Text Rebuttal — 한글 초안

LAMP의 핵심 아이디어를 인정해 주시고 논문의 주장과 인터페이스가 불명확한 부분을 지적해 주신 리뷰어들께 감사드립니다. 저희도 기존 연구와의 비교 및 입력 인코딩의 유효성이 가장 우선적인 문제라는 데 동의합니다.

**기존 연구 대비 위치와 비교.** LAMP는 범용 IOP나 end-to-end zkML 시스템이 아니라 certified matrix-product commitment를 위한 특화된 검사기입니다. Freivalds와 interleaved codeword의 sampled fold를 결합하고, CP-SNARK와 CP-Link로 샘플링된 값을 challenge 이전의 commitment에 연결합니다. Ligero, Brakedown, Blaze, Orion은 code testing의 기반이며, statement 수준에서 가장 가까운 비교 대상은 zkMatrix와 zkMaP입니다. zkCNN, zkGPT, zkLLM은 더 넓은 neural-network computation을 증명합니다.

Freivalds-in-Groth16은 동일한 relation, curve, backend, 하드웨어 및 보안 파라미터에서 LAMP reduction의 효과만 분리하기 위해 선택했습니다. 그러나 이 통제된 기준선만으로는 state of the art와의 비교가 충분하지 않습니다. Section 2와 Section 7에 statement, prover work, circuit cost, verifier work, proof size, setup, assumption 및 input model 비교표를 추가하겠습니다. LAMP, zkMatrix, zkMaP는 모두 quadratic native prover work를 필요로 하지만 LAMP는 in-circuit relation을 `O(tk + E_code)` constraint로 줄입니다. 대신 LAMP는 `O(t log n)` Merkle work와 `O(t)` QALink pairing term을 사용하고, zkMatrix와 zkMaP는 각각 logarithmic 및 constant verifier time을 보고하므로 verifier와 proof의 trade-off는 불리합니다. Statement와 security model의 차이를 설명하고 cross-platform 시간을 직접 비교하지 않겠습니다. Parameter를 맞출 수 있을 때만 정규화된 측정값을 추가하며, 그렇지 않으면 complexity 표와 실험 비교가 오해를 일으키는 이유를 제시하겠습니다.

**커밋된 행렬 인코딩의 유효성.** 리뷰어의 지적이 맞습니다. 현재 online relation은 intermediate vector에만 `EncCheck`를 적용하며, `A,B,C`의 row-wise encoding 유효성은 LAMP 자체가 증명하지 않습니다. 현재 sampled fold도 이 요구사항을 대신하지 못합니다. 저희가 의도한 것은 certified-input interface였지만, 원고에서 이 경계를 충분히 명확하게 정의하지 못했습니다. 수정본에서는 `rtABC`가 유효한 row-wise encoding과 그 opening을 가진 행렬에 커밋한다는 사실을 증명하는 certification relation `R_cert`를 정의하겠습니다. Proof 기반 인증을 사용하면 수정된 정리에서 `epsilon_total <= epsilon_cert + epsilon_LAMP` 형태로 오류를 합성하겠습니다. 신뢰할 수 있는 registration을 사용하는 방식은 encoder 또는 registrar의 정직성을 명시적 가정으로 두는 별도 instantiation이며, 이를 cryptographic certification과 혼동하지 않겠습니다. 두 인터페이스 중 어느 것도 없다면 arbitrary root는 주장하는 language의 범위 밖입니다. 이 경계를 Abstract, construction 및 security analysis 전체에 명시하겠습니다.

Raw matrix를 같은 SNARK 안에서 사용해야 한다면 직접적인 연결 방법은 `3k`개의 encoded row 각각을 대응하는 raw row와 검사하는 것입니다. 현재 barycentric Reed--Solomon check에서는 fixed rate일 때 `O(k(k+n)) = O(k^2)` constraint가 추가되므로 linear circuit 이점이 사라집니다. 대안으로 재사용 가능한 certification proof가 raw matrix와 encoded root를 한 번 연결할 수 있지만, 그 비용은 해당 root가 실제로 반복 사용될 때만 amortize됩니다. 이 조건을 가정하지 않고 명시적으로 밝히겠습니다. 이러한 bridge는 weight 또는 activation이 nonlinear layer에도 사용되는 end-to-end zkML에서 필요합니다. 따라서 LAMP를 certified encoded commitment가 이미 존재할 때 가장 유리한 online checker로 설명하고, certification 비용과 측정된 online 비용을 분리하며, `rtABC`가 실제로 의도한 model weight인지 인증하는 것은 응용 계층의 provenance 책임임을 명확히 하겠습니다.

**보안 범위와 setup.** 현재 정리는 public-coin interactive protocol을 증명합니다. 두 단계의 transcript 순서와 domain separation을 명시하고, non-interactive 주장을 random-oracle model에서 정당화할 수 있는 범위로 제한하겠습니다. Groth16의 per-circuit setup과 QALink의 configuration-dependent CRS도 설명하고, 비용을 보고하거나 배포상의 제한으로 명시하겠습니다.

**측정값과 서술 수정.** 정확한 값은 `0.06 s`, `8.43x`, `1.77x`이며 불일치하는 모든 표현을 수정하겠습니다. “Linear”는 전체 prover time이 아니라 핵심 constraint count를 의미합니다. Fixed code rate에서 intermediate computation과 encoded-column commitment는 여전히 quadratic입니다. GPT-2 결과도 prover 성능만 강조하지 않고 `1.77x` 빠른 proving 대신 verification이 `0.49 s`에서 `6.52 s`로, proof가 `196 B`에서 `3.34 MB`로 증가하는 trade-off로 보고하겠습니다. Proof가 커지는 이유는 Merkle opening과 CP-Link가 추가되기 때문입니다. Encoding time의 집계 방식, 가능한 범위의 반복 측정·분산·메모리를 보완하고, Theorem 6.2의 proof와 appendix 연결도 강화하겠습니다.
