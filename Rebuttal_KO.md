# IEEE S&P 2027 Text Rebuttal — 한글 초안

LAMP의 핵심 아이디어를 인정해 주시고 논문의 주장과 인터페이스가 불명확한 부분을 지적해 주신 리뷰어들께 감사드립니다. 저희도 기존 연구와의 비교 및 입력 인코딩의 유효성이 가장 우선적인 문제라는 데 동의합니다.

**기존 연구 대비 위치와 비교.** LAMP는 범용 IOP나 end-to-end zkML 시스템이 아니라 certified encoded matrix-product commitment를 위한 특화된 online checker입니다. Ligero, Brakedown, Blaze, Orion은 관련 code-testing 기반을 제공하지만 더 일반적인 relation을 증명하며, zkCNN, zkGPT, zkLLM은 더 넓은 neural-network computation을 대상으로 합니다. 서로 다른 end-to-end 실행시간을 동일한 지표처럼 비교하지 않고 statement와 input model을 개념적으로 비교하며, LAMP의 기여가 code testing 자체가 아니라 matrix multiplication에 특화된 reduction임을 명확히 하겠습니다.

Freivalds-in-Groth16은 동일한 relation, curve, backend, 하드웨어 및 보안 파라미터에서 LAMP reduction의 효과를 분리하지만 state-of-the-art baseline은 아닙니다. zkMatrix는 `O(k^2)` prover work와 `O(log k)` transcript/verifier를 보고합니다. DualMatrix는 `K=nnz(A)+nnz(B)+nnz(C)`일 때 `O(K+k)` field operation과 `O(k)` group operation/pairing을 사용하며, dense input에서는 `K=Theta(k^2)`이고 transcript/verifier는 `O(log k)`입니다. zkMaP은 `O(k^2)` prover work, 320-byte proof 및 constant pairing count를 보고하지만, 상세 분석에는 `O(k)` verifier-side field work도 포함됩니다. Fixed code rate에서 LAMP는 intermediate vector 계산에 `O(k^2)` field work와 encoded commitment에 `O(k^2)` group work가 필요하지만, in-circuit relation은 `O(tk + E_code)` constraint로 줄입니다. 8월 26일 현재 zkMatrix 공개 구현을 찾지 못했고 zkMaP의 artifact link도 사용할 수 없어 저자들에게 코드를 요청했으며, 후속 연구인 DualMatrix의 공개 구현을 평가했습니다. Dense matrix의 `k=1024`에서 DualMatrix/LAMP의 proving time은 `19.06/7.11 s`, verification time은 `0.15/0.06 s`, proof size는 `88,844/43,332 B`였으며, `k=128`부터 `k=8,192`까지 모든 평가 지점에서 LAMP가 세 지표 모두 더 낮았습니다. Section 2와 Section 7에 이 결과와 statement, operation 종류, circuit/verifier cost, proof size, setup, assumption, input model 비교표를 추가하고 hardware, curve, security, implementation 및 cost boundary 차이도 명시하겠습니다.

**커밋된 행렬 인코딩의 유효성.** 리뷰어의 지적이 맞습니다. 현재 online relation은 intermediate vector에만 `EncCheck`를 적용하며, `A,B,C`의 row-wise encoding 유효성은 LAMP 자체가 증명하지 않습니다. 현재 sampled fold도 이 요구사항을 대신하지 못합니다. 저희가 의도한 것은 certified-input interface였지만, 원고에서 이 경계를 충분히 명확하게 정의하지 못했습니다. 수정본에서는 `rtABC`가 유효한 row-wise encoding과 그 opening을 가진 행렬에 커밋한다는 사실을 증명하는 certification relation `R_cert`를 정의하겠습니다. Proof 기반 인증을 사용하면 수정된 정리에서 `epsilon_total <= epsilon_cert + epsilon_LAMP` 형태로 오류를 합성하겠습니다. 신뢰할 수 있는 registration을 사용하는 방식은 encoder 또는 registrar의 정직성을 명시적 가정으로 두는 별도 instantiation이며, 이를 cryptographic certification과 혼동하지 않겠습니다. 두 인터페이스 중 어느 것도 없다면 arbitrary root는 주장하는 language의 범위 밖입니다. 이 경계를 Abstract, construction 및 security analysis 전체에 명시하겠습니다.

Raw matrix를 같은 SNARK 안에서 사용한다면 `3k`개의 encoded row를 대응하는 raw row와 barycentric Reed--Solomon 방식으로 검사해야 합니다. Fixed rate에서는 `O(k(k+n)) = O(k^2)` constraint가 추가되어 linear-circuit 이점이 사라집니다. 재사용 가능한 certification proof로 한 번 연결할 수 있지만, amortization은 root가 반복 사용될 때만 가능합니다. 이러한 bridge는 weight나 activation이 nonlinear layer에도 사용되는 end-to-end zkML에서 필요합니다. LAMP를 이미 인증된 commitment를 위한 online checker로 설명하고, certification과 online 측정 비용을 분리하며, `rtABC`의 model-weight authenticity는 응용 계층의 provenance 문제임을 밝히겠습니다.

**보안 범위와 setup.** 현재 정리는 public-coin interactive protocol을 증명합니다. 두 단계의 transcript 순서와 domain separation을 명시하고, non-interactive 주장을 random-oracle model에서 정당화할 수 있는 범위로 제한하겠습니다. Groth16의 per-circuit setup과 QALink의 configuration-dependent CRS도 설명하고, 비용을 보고하거나 배포상의 제한으로 명시하겠습니다.

**측정값과 서술 수정.** 정확한 값은 `0.06 s`, `8.43x`, `1.77x`이며 불일치하는 모든 표현을 수정하겠습니다. “Linear”는 전체 prover time이 아니라 핵심 constraint count를 의미합니다. Fixed code rate에서 intermediate computation과 encoded-column commitment는 여전히 quadratic입니다. GPT-2 결과도 prover 성능만 강조하지 않고 `1.77x` 빠른 proving 대신 verification이 `0.49 s`에서 `6.52 s`로, proof가 `196 B`에서 `3.34 MB`로 증가하는 trade-off로 보고하겠습니다. Proof가 커지는 이유는 Merkle opening과 CP-Link가 추가되기 때문입니다. Encoding time의 집계 방식, 가능한 범위의 반복 측정·분산·메모리를 보완하고, Theorem 6.2의 proof와 appendix 연결도 강화하겠습니다.
