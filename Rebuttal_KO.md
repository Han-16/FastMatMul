# IEEE S&P 2027 Text Rebuttal — 한글 초안

세심하고 건설적인 피드백을 주신 리뷰어들께 감사드립니다.

**기존 연구 대비 위치와 비교.** LAMP는 범용 IOP나 end-to-end zkML 시스템이 아니라 encoded matrix-product commitment를 위한 특화된 checker입니다. Ligero, Brakedown, Blaze, Orion은 더 일반적인 code-testing 기반을 제공하고, zkCNN, zkGPT, zkLLM은 더 넓은 neural-network computation을 대상으로 합니다. End-to-end 실행시간을 동일한 지표처럼 비교하지 않고 statement와 input model을 비교하며, 기여가 code testing 자체가 아니라 matrix multiplication에 특화된 reduction임을 명확히 하겠습니다.

Freivalds-in-Groth16은 동일한 relation, curve, backend, 하드웨어 및 보안 파라미터에서 LAMP reduction의 효과를 분리하지만 state-of-the-art baseline은 아닙니다. zkMatrix는 `O(k^2)` prover work와 `O(log k)` transcript/verifier를 보고합니다. DualMatrix는 `K=nnz(A)+nnz(B)+nnz(C)`일 때 `O(K+k)` field operation과 `O(k)` group operation/pairing을 사용하며, dense input에서는 `K=Theta(k^2)`이고 transcript/verifier는 `O(log k)`입니다. zkMaP은 `O(k^2)` prover work, 320-byte proof 및 constant pairing count를 보고하지만, 상세 분석에는 `O(k)` verifier-side field work도 포함됩니다. Fixed code rate에서 LAMP는 intermediate vector 계산에 `O(k^2)` field work와 encoded commitment에 `O(k^2)` group work가 필요하지만, in-circuit relation은 `O(tk + E_code)` constraint로 줄입니다. zkMatrix 공개 구현을 찾지 못했고 zkMaP의 artifact도 사용할 수 없었습니다. 짧은 rebuttal 기간에 두 시스템을 새로 구현하는 것은 신뢰할 수 있는 동일 조건 비교를 보장하기 어렵습니다. 따라서 두 시스템과는 점근적 복잡도를 비교하고, 공개 구현이 있는 zkMatrix의 후속 연구 DualMatrix를 가장 가까운 재현 가능한 실험 대상으로 평가했습니다. `k=1024`에서 DualMatrix/LAMP의 proving time은 `19.06/7.11 s`, verification time은 `0.15/0.06 s`, proof size는 `88,844/43,332 B`였습니다. 현재 구현된 경로에서는 `k=128`부터 `k=8,192`까지 LAMP가 세 지표 모두 낮았습니다. 이 측정값은 추가 `B` 검사를 반영하기 전 결과이므로 갱신할 예정이며, 수정 프로토콜의 최종 end-to-end 결과로 주장하지 않습니다. Section 2와 Section 7에 statement, operation 종류, 비용, proof size, setup, assumption, input model 및 실험 조건의 비교표를 추가하겠습니다.

**커밋된 행렬 인코딩의 유효성.** Reviewer C와 D의 지적대로 현재 정리는 입력 codeword의 유효성을 증명하지 않고 가정합니다. 직관적으로 Brakedown은 행들이 commit된 후 무작위 선형결합을 만들어 많은 encoded row를 한 번에 검사합니다. Commit된 행들이 유효한 row-wise encoding에 가깝지 않다면, 작은 확률을 제외하고 결합된 word도 code에서 멀며 무작위 좌표 질의가 그 차이를 발견합니다. 반대로 충분히 가깝다면 대응하는 decoded row를 추출할 수 있습니다. `A`와 `C`에 대해서는 structured challenge `v(r)`를 위한 별도의 Reed--Solomon proximity lemma를 증명하겠습니다. `B` fold는 계수가 `x=v(r)A`이므로 다릅니다. Rank-deficient `A`는 kernel에 있는 잘못된 `B` component를 숨길 수 있습니다. 이를 해결하기 위해 `B`에 독립적인 Brakedown-style test를 추가하겠습니다. `rtABC`가 고정된 후 verifier가 독립적인 무작위 벡터 `gamma_B`를 새로 선택하고, prover는 query position이 선택되기 전에 `b_gamma=gamma_B B`와 그 encoding에 commit합니다. `EncCheck`는 folded encoding을 확인하고, sampled equation `hat(b_gamma)[j]=<gamma_B,hat(B)[*,j]>`은 이것을 committed `B`와 연결합니다. 수정된 정리에는 proximity/extraction bound를 명시하고, 비용 분석에는 추가 encoded fold, `EncCheck`, `O(tk)` sampled constraint 및 `O(k)` verifier challenge 처리를 포함하겠습니다.

Raw matrix를 같은 SNARK 안에서 사용한다면 decoded matrix와 raw witness를 추가로 연결해야 합니다. 한 가지 직접적인 방법은 `3k`개 row를 검사하는 것이며, fixed rate에서 `O(k(k+n)) = O(k^2)` constraint가 추가되어 linear-circuit 이점이 사라집니다. Weight나 activation이 nonlinear relation에도 사용되는 경우 이러한 연결이 필요합니다. 이 비용을 별도로 보고하고, `rtABC`가 지정된 weight임을 인증하는 문제는 application-level provenance로 구분하겠습니다.

**보안 범위와 setup.** Formal theorem은 public-coin interactive protocol로 한정하고, 구현된 multi-round Fiat--Shamir transform의 정식 ROM 분석은 future work로 남기겠습니다. Groth16의 per-circuit setup과 QALink의 configuration-dependent CRS 및 배포 비용도 설명하겠습니다.

**측정값과 서술 수정.** 정확한 값은 `0.06 s`, `8.43x`, `1.77x`이며 모든 불일치를 수정하겠습니다. “Linear”는 전체 prover time이 아니라 핵심 constraint count를 의미하며, fixed code rate에서 intermediate computation과 encoded-column commitment는 여전히 quadratic입니다. GPT-2 결과는 `1.77x` 빠른 proving 대신 verification이 `0.49 s`에서 `6.52 s`로, proof가 `196 B`에서 `3.34 MB`로 증가하는 trade-off로 보고하겠습니다. `196 B`는 오타가 아닙니다. Groth16 proof size는 circuit size와 무관하게 constant이고, LAMP는 sampled Merkle opening과 CP-Link proof도 직렬화합니다. Encoding time, 반복 측정·분산·메모리를 보완하고 Theorem 6.2의 full proof를 appendix에 포함하겠습니다.
